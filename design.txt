HTML:
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OMNIGENE</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #5f6cff, #a96eff);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Orbitron', sans-serif;
    }

    .container {
      text-align: center;
      transition: opacity 0.8s ease-in-out;
    }

    h1 {
      font-size: 6rem;
      text-transform: uppercase;
      color: transparent;
      -webkit-text-stroke: 2px white;
      margin-bottom: 5px;
    }

    p {
      color: white;
      font-size: 2.3rem;
      font-family: Times New Roman;
      margin: 10px 0;
    }

    button {
      padding: 10px 20px;
      font-size: 1.5rem;
      border: none;
      border-radius: 6px;
      background-color: white;
      color: #906cfc;
      cursor: pointer;
      margin-top: 69px;
      transition: transform 0.2s;
    }

    button:hover {
      transform: scale(1.05);
    }

    .fade-in-element {
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
    }

    .search-container {
      display: flex;
      align-items: center;
      width: 80%;
      max-width: 500px;
      margin: 0 auto;
    }

    #search-bar, #compact-search-bar {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      flex-grow: 1;
      font-size: 1rem;
      transition: opacity 0.8s ease-in-out;
    }

    .search-button {
      background-color: white;
      border: 1px solid #ccc;
      border-radius: 6px;
      padding: 6px;
      margin-left: 10px;
      margin-top: 0px;
      cursor: pointer;
      width: 40px; 
      height: 40px;
    }

    .search-button svg {
      width: 20px;
      height: 20px;
      fill: #5f6cff;
    }

    #compact-search-container {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      width: 80%;
      max-width: 500px;
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
      z-index: 1000;
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 3rem;
        -webkit-text-stroke: 1px white;
      }
      #compact-search-container {
        width: 90%;
      }
    }
  </style>
</head>
<body>
  <div class="container" id="initial-content">
    <h1>OMNIGENE</h1>
    <p>"All the genes, one place"</p>
    <button id="tryMeBtn">Try me🧬</button>
  </div>

  <div id="fade-in-content" style="display: none; align-items: center; flex-direction: column; margin-top: 30px;">
    <p id="smart-fast-easy" class="fade-in-element slogan-text">smart, fast & easy</p>
    <div class="search-container">
      <input type="text" id="search-bar" placeholder="Enter your gene" class="search-placeholder">
      <button class="search-button">
        <svg viewBox="0 0 24 24">
          <path d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
        </svg>
      </button>
    </div>
    <p id="next-gen-search" class="fade-in-element next-gen-text">next-gen search tool</p>
  </div>

  <div id="compact-search-container">
    <div class="search-container">
      <input type="text" id="compact-search-bar" class="search-placeholder" placeholder="Enter your gene">
      <button class="search-button" id="compact-search-btn">
        <svg viewBox="0 0 24 24">
          <path d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
        </svg>
      </button>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Elemente DOM
      const tryMeBtn = document.getElementById('tryMeBtn');
      const initialContent = document.getElementById('initial-content');
      const fadeInContent = document.getElementById('fade-in-content');
      const searchBar = document.getElementById('search-bar');
      const searchButton = document.querySelector('.search-button');
      const compactSearchContainer = document.getElementById('compact-search-container');
      const compactSearchBar = document.getElementById('compact-search-bar');
      const compactSearchBtn = document.getElementById('compact-search-btn');

      // 1. Ascunde conținutul inițial și afișează bara de căutare principală
      tryMeBtn.addEventListener('click', function() {
        initialContent.style.opacity = '0';
        setTimeout(() => {
          initialContent.style.display = 'none';
          fadeInContent.style.display = 'flex';
          animateElements();
        }, 550);
      });

      // 2. Funcție pentru animații fade-in
      function animateElements() {
        document.querySelectorAll('.fade-in-element').forEach((el, index) => {
          setTimeout(() => el.style.opacity = '1', 300 * (index + 1));
        });
        setTimeout(() => {
          searchBar.style.opacity = '1';
          searchButton.style.opacity = '1';
        }, 600);
      }

      // 3. Funcție universală pentru căutare
      function handleSearch(inputElement, isCompact = false) {
        const gene = inputElement.value.trim();
        if (!gene) return;

        // Fade-out la conținutul curent
        if (!isCompact) {
          fadeInContent.style.opacity = '0';
          setTimeout(() => fadeInContent.style.display = 'none', 800);
        }

        // Fade-in pentru bara compactă
        compactSearchBar.value = gene;
        if (compactSearchContainer.style.display !== 'block') {
          compactSearchContainer.style.display = 'block';
          setTimeout(() => compactSearchContainer.style.opacity = '1', 50);
        }

        // Aici poți adăuga logica pentru afișarea rezultatelor
        console.log(`Searching for: ${gene}`);
        // Exemplu: fetchResults(gene);
      }

      // 4. Evenimente de căutare
      searchButton.addEventListener('click', () => handleSearch(searchBar));
      searchBar.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') handleSearch(searchBar);
      });

      compactSearchBtn.addEventListener('click', () => handleSearch(compactSearchBar, true));
      compactSearchBar.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') handleSearch(compactSearchBar, true);
      });
    });
  </script>
  </body>
</html>

CSS:
body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(135deg, #5f6cff, #a96eff);
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: 'Orbitron', sans-serif;
}

/* Container initial */
.container {
  text-align: center;
  transition: opacity 0.8s ease-in-out;
}

h1 {
  font-size: 6rem;
  text-transform: uppercase;
  color: transparent;
  -webkit-text-stroke: 2px white;
  margin-bottom: 5px;
}

p {
  color: white;
  font-size: 1.9rem;
  margin: 10px 0;
}

button {
  padding: 10px 20px;
  font-size: 1.5rem;
  border: none;
  border-radius: 6px;
  background-color: white;
  color: #906cfc;
  cursor: pointer;
  margin-top: 69px;
  transition: transform 0.2s;
  font-family: 'Orbitron', sans-serif;
}

button:hover {
  transform: scale(1.05);
}

/* Elemente fade-in */
.fade-in-element {
  opacity: 0;
  transition: opacity 0.8s ease-in-out;
}

.slogan-text {
  font-size: 2rem;
  font-family: 'Orbitron', sans-serif;
  font-weight: bold;
}

.next-gen-text {
  font-size: 1.2rem;
  font-family: 'Orbitron', sans-serif;
  margin-top: 10px;
}

/* Bara de căutare */
.search-container {
  display: flex;
  align-items: center;
  width: 80%;
  max-width: 500px;
}

#search-bar, #compact-search-bar {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 6px;
  flex-grow: 1;
  font-size: 1rem;
  transition: opacity 0.8s ease-in-out;
}

.search-button {
  background-color: white;
  border: 1px solid #ccc;
  border-radius: 6px;
  padding: 8px;
  margin-left: 10px;
  cursor: pointer;
}

.search-button svg {
  width: 20px;
  height: 20px;
  fill: #5f6cff;
}

/* Bara compactă */
#compact-search-container {
  transition: opacity 0.8s ease-in-out;
  z-index: 1000;
}

#compact-search-bar {
  width: 100%;
  background-color: rgba(255, 255, 255, 0.9);
}

/* Responsiv */
@media (max-width: 768px) {
  h1 {
    font-size: 3rem;
    -webkit-text-stroke: 1px white;
  }
  #compact-search-container {
    width: 90%;
  }
}

JS:
document.addEventListener('DOMContentLoaded', function() {
  // Elemente DOM
  const tryMeBtn = document.getElementById('tryMeBtn');
  const initialContent = document.getElementById('initial-content');
  const fadeInContent = document.getElementById('fade-in-content');
  const searchBar = document.getElementById('search-bar');
  const searchButton = document.querySelector('.search-button');
  const compactSearchContainer = document.getElementById('compact-search-container');
  const compactSearchBar = document.getElementById('compact-search-bar');
  const resetSearchBtn = document.getElementById('reset-search');

  // 1. Ascunde conținutul inițial și afișează bara de căutare
  tryMeBtn.addEventListener('click', function() {
    initialContent.style.opacity = '0';
    setTimeout(() => {
      initialContent.style.display = 'none';
      fadeInContent.style.display = 'flex';
      
      // Animație fade-in pentru elemente
      document.querySelectorAll('.fade-in-element').forEach((el, index) => {
        setTimeout(() => {
          el.style.opacity = '1';
        }, 300 * (index + 1));
      });
      
      setTimeout(() => {
        searchBar.style.opacity = '1';
        document.querySelector('.search-button').style.opacity = '1';
      }, 600);
    }, 550);
  });

  // 2. La căutare, mută bara în partea de sus
  searchButton.addEventListener('click', handleSearch);
  searchBar.addEventListener('keypress', function(e) {
    if (e.key === 'Enter') handleSearch();
  });

  function handleSearch() {
    const gene = searchBar.value.trim();
    if (!gene) return;

    // Fade-out la conținutul curent
    fadeInContent.style.opacity = '0';
    
    setTimeout(() => {
      fadeInContent.style.display = 'none';
      
      // Setează valoarea în bara compactă
      compactSearchBar.value = gene;
      compactSearchContainer.style.display = 'block';
      
      // Fade-in pentru bara compactă
      setTimeout(() => {
        compactSearchContainer.style.opacity = '1';
        
        // Aici poți adăuga logica pentru afișarea rezultatelor
        console.log(`Searching for: ${gene}`);
      }, 50);
    }, 800);
  }

  // 3. Resetare la starea inițială
  resetSearchBtn.addEventListener('click', function() {
    compactSearchContainer.style.opacity = '0';
    setTimeout(() => {
      compactSearchContainer.style.display = 'none';
      initialContent.style.display = 'flex';
      initialContent.style.opacity = '1';
      searchBar.value = '';
    }, 800);
  });
});